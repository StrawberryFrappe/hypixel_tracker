# C4 Architecture

- Status: Target architecture and staged implementation plan accepted 2026-07-13
- Updated: 2026-07-13
- Product: Bazaar Guru
- Notation: Mermaid flowcharts using C4 person, system, container, and component
  boundaries.

Target architecture appears first. Legacy system appears only in appendix and
must not be mistaken for desired product.

## Target: Level 1 System Context

```mermaid
flowchart LR
    Admin[Person: Administrator\nPlays SkyBlock with Bazaar Guru on second monitor]
    Hypixel[External system: Hypixel Public API]
    Desktop[External system: Gaming desktop storage\nDownloaded weekly recovery sets]
    Training[External system: Development workstation\nOffline dataset preparation and model training]
    Guru[System: Bazaar Guru\nLAN-only unofficial trading assistant]

    Admin -->|Local HTTPS: analyzes markets, exports, administers recovery| Guru
    Guru -->|Respectful cached collection| Hypixel
    Guru -->|Downloads independent recovery sets| Desktop
    Desktop -->|Uploads selected recovery sets| Guru
    Guru -->|Exports versioned datasets| Training
    Training -->|Returns reviewed model artifact| Guru
```

## Target: Level 2 Containers

This view contains two PostgreSQL containers by explicit user decision.

```mermaid
flowchart TB
    Admin[Person: Administrator]
    Hypixel[External: Bazaar and item APIs]
    Desktop[External: Gaming desktop backup storage]
    Training[External: Development workstation trainer]
    CLI[Administrator tool: Recovery CLI]

    subgraph Host[System boundary: Ubuntu deployment laptop]
        Edge[Container: Caddy HTTPS edge\nLAN bind, local CA, routing, static files]
        Web[Container content: Bazaar Guru Vue 3 Web App\nMarket terminal, product charts, datasets, operations]
        API[Container: FastAPI application API\nAdmin session, scoped keys, queries, controls]
        Collector[Container: Collector\nAdaptive poll, validate, hash, compress, record]
        Worker[Container: Curator and scheduler worker\nOrdered transform, leases, aggregates, lineage]
        Recovery[On-demand container profile: Recovery worker\nRuns only while heavy worker is stopped]
        Predictor[Future container: Prediction service\nAccepted models only]

        RawDB[(Container: Raw PostgreSQL\nFetch ledger, source revisions, compressed exact bodies)]
        AppDB[(Container: Application PostgreSQL\nProducts, curated history, auth, jobs, settings, operations)]
        Parquet[(Data store: Curated Parquet archive on /srv\nImmutable ML-ready generations)]
        Staging[(Data store: Bounded staging on /srv\nExports, recovery sets, quarantine, rollback)]
        Registry[(Future data store: Model registry\nReviewed artifacts and reports)]
    end

    Admin --> Edge
    Edge --> Web
    Edge --> API
    Web -->|Authenticated /api/v1 calls| API

    Collector -->|HTTPS, minimum 10-second spacing| Hypixel
    Collector -->|One raw transaction per observation| RawDB

    Worker -->|Oldest pending source revision| RawDB
    Worker -->|Staged and visible serving generations plus operations summaries| AppDB
    Worker -->|Finalized immutable generations| Parquet
    Worker -->|Immutable completion and prune facts| RawDB

    Worker -->|Durable leases, retries, dead letters, singleton schedules| AppDB

    API -->|Serving, auth, settings, operations only| AppDB
    API -->|Protected job requests| AppDB
    API -.->|Release 3 accepted-model query| Predictor
    Predictor -->|Current features| AppDB
    Predictor -->|Accepted artifact| Registry

    RawDB -->|Published-watermark raw export| Recovery
    AppDB -->|Published serving/control snapshot| Recovery
    Parquet -->|Finalized generations and manifests| Recovery
    Recovery -->|Temporary bounded files| Staging
    Staging -->|Bounded stream source| API
    API -->|Authenticated range response| Edge
    Edge -->|HTTPS recovery-set download| Desktop
    Desktop -->|Administrator-selected upload| Edge
    CLI -->|Authenticated HTTPS recovery command| Edge

    Parquet -->|Versioned dataset job output| Staging
    Edge -->|Authenticated dataset download| Training
    Training -->|Reviewed model upload| Edge
    API -->|Registers accepted artifact| Registry
```

## Target: Raw Collection Components

```mermaid
flowchart LR
    Hypixel[Hypixel Bazaar API]

    subgraph Collector[Collector container]
        Cadence[Cadence controller\n10-second minimum, phase estimate, backoff]
        Client[Bounded HTTPS client\nHost allowlist, timeout, response caps]
        Capture[Bounded body capture\nHeaders, decoded bytes, size and redirect policy]
        Identity[Identity builder\nDecoded-body SHA-256 and observation sequence]
        Compression[Deterministic compressor]
        Repository[Capture repository\nAtomic fetch, body, observation commit]
        Validator[Persisted-body validator\nIdempotent semantic result and source revision]
    end

    RawDB[(Raw PostgreSQL)]

    Cadence --> Client --> Hypixel
    Hypixel --> Client --> Capture --> Identity --> Compression --> Repository
    Capture -->|Metadata-only cap or transport rejection| Repository
    Repository -->|fetch_attempt| RawDB
    Repository -->|deduplicated raw_body| RawDB
    Repository -->|unvalidated observation sequence| RawDB
    RawDB -->|persisted bounded bytes| Validator
    Validator -->|validation result and source_update revision| RawDB
    RawDB -->|last observations and errors| Cadence
```

Initial Raw PostgreSQL transaction records attempt metadata, body identity,
compressed exact decoded entity bytes, and observation sequence. Validator reads
only persisted bytes, then writes semantic result and source revision in a
separate idempotent transaction. Parser failure cannot lose captured body. Every
bounded received body is captured before semantic validation, including malformed
or schema-changing JSON. Body omission is allowed only when transport produces
none or configured cap rejects it, with rejection metadata retained. Same
`lastUpdated` with changed body hash becomes a new revision.

## Target: Curation and Two-Database Consistency

```mermaid
flowchart LR
    RawDB[(Raw PostgreSQL\nCanonical observation ledger and bodies)]
    AppDB[(Application PostgreSQL\nRebuildable serving projection)]
    Parquet[(Immutable Parquet generations\nDurable curated archive)]

    subgraph Curator[Curator worker]
        Scanner[Pending scanner\nOldest source sequence first]
        Parser[Versioned payload parser]
        Features[Feature calculator\nQuick status, depth, spread, liquidity]
        Metadata[Metadata enricher\nOfficial and derived names]
        Stage[Parquet generation stager\nTemporary file and checksum]
        Finalize[Parquet finalizer\nfsync, atomic rename, signed manifest]
        Projection[Application staging transaction\nRows, operations summaries, generation not visible]
        RawPublish[Raw publication transaction\nParquet ready plus projection ready]
        AppPublish[Application publication transaction\nGeneration becomes API-visible]
        Rebuild[Projection rebuilder\nParquet manifests to Application PostgreSQL]
        Reconcile[Crash reconciler\nResume or remove incomplete states]
    end

    RawDB --> Scanner --> Parser
    Parser --> Features
    Parser --> Metadata
    Features --> Stage
    Metadata --> Stage
    Stage --> Finalize --> Parquet
    Features --> Projection --> AppDB
    Metadata --> Projection
    Finalize --> RawPublish
    Projection --> RawPublish
    RawPublish -->|immutable completion, generation ID, prune candidate| RawDB
    RawPublish --> AppPublish --> AppDB
    Parquet --> Rebuild --> AppDB
    Reconcile --> RawDB
    Reconcile --> AppDB
    Reconcile --> Parquet
```

No distributed transaction is claimed. Saga states are `captured`,
`parquet_ready`, `projection_ready`, `raw_published`, and `app_visible`. API
queries only Application generations marked visible in one transaction. Curator
projects gap, failure, lag, and high-watermark summaries into Application
PostgreSQL while immutable completion/prune facts remain in Raw PostgreSQL.
Reconciliation tests every crash boundary. Parquet manifests contain every field
needed to rebuild Application projections after raw pruning. Raw data becomes
prune-eligible only after `app_visible` and rebuild verification.

## Target: Web, API, and Forecast Components

```mermaid
flowchart TB
    Browser[Administrator browser]
    Client[Scoped automation client]

    subgraph Edge[HTTPS edge]
        TLS[Local TLS termination]
        Routes[LAN routing and request limits]
    end

    subgraph Vue[Bazaar Guru Vue 3 Web App]
        Overview[Market overview]
        Product[Product explorer and charts]
        Datasets[Dataset and recovery downloads]
        Operations[Health, storage, gaps, jobs, restore]
        Settings[Trade profile and versioned fees]
        Status[Freshness and degraded-state presentation]
    end

    subgraph App[FastAPI application]
        Auth[Authentication\nAdmin session and scoped API keys]
        Dispatch[Authorized dispatcher\nSession, CSRF, Origin, scope checks]
        Market[Market query service]
        Rank[Opportunity ranking\nFill, slippage, fees, return, volatility]
        Freshness[Presentation guard\nFreshness, gaps, resolution, degraded state]
        Export[Dataset and recovery download service]
        Ops[Operations service]
        Restore[Admin-only restore controller]
        Config[Settings and fee-version service]
        Forecast[Future accepted-model adapter]
    end

    AppDB[(Application PostgreSQL)]
    Jobs[Durable job scheduler]
    Predictor[Future prediction service]

    Browser --> TLS --> Routes --> Vue
    Client --> TLS
    Routes --> Auth
    Overview -->|Authenticated request| Auth
    Product -->|Authenticated request| Auth
    Datasets -->|Authenticated request| Auth
    Operations -->|Authenticated request| Auth
    Settings -->|Authenticated request| Auth
    Auth --> Dispatch
    Dispatch --> Market
    Dispatch --> Export
    Dispatch --> Ops
    Dispatch --> Restore
    Dispatch --> Config
    Market --> AppDB
    Market --> Rank
    Rank --> AppDB
    Market --> Freshness
    Rank --> Freshness
    Freshness --> Overview
    Freshness --> Product
    Freshness --> Status
    Export --> Jobs
    Ops --> AppDB
    Ops --> Jobs
    Restore --> Jobs
    Config --> AppDB
    Export --> Datasets
    Ops --> Operations
    Restore --> Operations
    Config --> Settings
    Forecast -.->|Release 3 only| Predictor
    Predictor -.->|Prediction, uncertainty, provenance| Forecast
    Predictor -.-> AppDB
    Forecast -.->|Accepted signal only| Rank
```

The webpage is a first-class Vue application, not Adminer or generated API docs.
Forecast output cannot bypass transparent non-model score components or freshness
guards.

## Target: Backup and Restore Components

```mermaid
flowchart TB
    Browser[Administrator browser]
    CLI[Administrator recovery CLI]
    SSH[Hardened SSH management boundary\nLocal administrator only, no artifact transfer]
    Desktop[Gaming desktop recovery-set storage]
    USB[USB critical recovery media]
    Edge[Caddy HTTPS edge]
    API[Authenticated FastAPI recovery controller]
    HostController[Restricted host recovery controller\nStarts profile, switches stores, no LAN listener]

    subgraph Recovery[Backup and recovery worker]
        Watermark[Published-generation watermark coordinator\nQuiesces curation, records LSNs and schema versions]
        RawDump[Raw time-range exporter]
        AppDump[Application control/projection exporter\nExcludes auth and secrets]
        Files[Parquet and manifest collector]
        Sign[Ed25519 manifest signer]
        Intake[Quarantine consumer]
        Verify[Fingerprint, signature, checksum, schema verifier]
        Quiesce[Mutating-job quiescence]
        Candidate[Candidate restore coordinator]
        CandidateBootstrap[Fresh candidate-admin bootstrap]
        Switch[Health-gated candidate switch]
        Rollback[Post-switch rollback controller]
        Cleanup[Candidate cleanup controller]
        Audit[Outcome and gap recorder]
        Resume[Job resume controller]
        Reconcile[Startup journal reconciler]
    end

    RawDB[(Active Raw PostgreSQL)]
    AppDB[(Active Application PostgreSQL)]
    Parquet[(Active curated Parquet archive)]
    CandidateRaw[(Candidate logical database\nInside capped Raw PostgreSQL)]
    CandidateApp[(Candidate logical database\nInside capped Application PostgreSQL)]
    CandidateParquet[(Candidate Parquet path)]
    Previous[(Previous active stores retained for rollback)]
    Stage[(Bounded staging and quarantine)]
    Journal[(Host fsynced recovery request and phase journal\nIndependent of both databases)]
    BootstrapFile[(Host 0600 one-time bootstrap file\nExpires, never logged)]
    Jobs[Collector and heavy worker]

    Watermark -->|Stops new curation publication| Jobs
    RawDB -->|Rows up to published source sequence| RawDump
    AppDB -->|Visible generation and nonsecret control state| AppDump
    Parquet -->|Finalized generations up to watermark| Files
    Watermark --> RawDump
    Watermark --> AppDump
    Watermark --> Files
    RawDump --> Sign
    AppDump --> Sign
    Files --> Sign
    Sign --> Stage
    Sign -->|Critical nonsecret control bundle| USB
    Sign -->|Backup success| Audit
    Reconcile -->|Recovers failed/crashed backup phase| Audit

    Stage -->|Bounded source for range response| API
    Browser --> Edge --> API
    CLI -->|Authenticated HTTPS, no direct worker access| Edge
    API -->|Authenticated download| Edge
    Edge -->|HTTPS range download| Desktop
    Desktop -->|HTTPS bounded upload| Edge
    Edge --> API
    API -->|Writes bounded upload to quarantine| Stage
    API -->|Authenticated restore request| Journal
    Journal --> HostController
    CLI -.->|Break-glass command| SSH --> HostController
    HostController -->|Stops Worker, mounts recovery credentials, starts profile| Intake
    Stage --> Intake --> Verify --> Quiesce
    Verify -->|Pre-switch failure| Cleanup
    Quiesce -->|Stops mutating jobs| Jobs
    Quiesce --> Candidate
    Candidate --> CandidateRaw
    Candidate --> CandidateApp
    Candidate --> CandidateParquet
    Candidate --> CandidateBootstrap --> CandidateApp
    CandidateBootstrap -->|Created before old session invalidation| BootstrapFile
    Candidate -->|Candidate build failure| Cleanup
    CandidateRaw --> Switch
    CandidateApp --> Switch
    CandidateParquet --> Switch
    RawDB --> Previous
    AppDB --> Previous
    Parquet --> Previous
    Switch -->|Verified success| Audit
    Switch -->|Post-switch failure| Rollback
    Previous --> Rollback --> Audit
    Cleanup --> Audit
    Audit -->|Persists every terminal outcome first| Journal
    Journal -->|Projects outcome when Application DB is available| AppDB
    Audit --> Resume -->|Restarts after success or rollback| Jobs
    Reconcile -->|Resumes or safely rolls back interrupted restore| HostController
```

Weekly recovery sets are independent UTC time ranges, not delta chains. Restore
can apply any complete set; missing sets become explicit gaps. Bundle-supplied
verification keys are ignored in favor of release-pinned fingerprint metadata.
Candidate stores are built and verified before switch. A failed candidate leaves
active stores and existing auth untouched; only a successful candidate receives
fresh bootstrap auth because backups exclude credentials. Candidate databases
are logical databases inside existing capped PostgreSQL containers, not two more
processes. Host journal, not Application PostgreSQL, is restore orchestration
authority. Every backup/restore success, failure, or recovered crash reaches
Audit and Resume. After successful switch, administrator retrieves one-time
bootstrap secret through protected host-local SSH path and rotates it immediately.

## Target: Deployment View

```mermaid
flowchart LR
    subgraph LAN[Private local network]
        Browser[Administrator browser]
        Desktop[Gaming desktop\nWeekly recovery downloads]
        Training[Development workstation\nOffline model training]

        subgraph Laptop[Deployment laptop\nUbuntu 26.04 LTS, 2 cores, 5.2 GiB RAM]
            Edge[Docker: Caddy plus Vue\n128 MiB target]
            API[Docker: FastAPI\n384 MiB target]
            Collector[Docker: Collector\n192 MiB target]
            Worker[Docker: Curator plus scheduler\n512 MiB target]
            Recovery[On-demand Docker profile: Recovery\n512 MiB; Worker stopped first]
            HostCtrl[Host service: Restricted recovery controller\nFsynced journal, no network listener]
            RawDB[Docker: Raw PostgreSQL\n768 MiB container ceiling]
            AppDB[Docker: Application PostgreSQL\n1 GiB container ceiling]
            Data["/srv data\nTwo DB volumes, Parquet, bounded staging"]
            CA[Persistent protected Caddy CA]
            Journal["/srv recovery journal and one-time bootstrap file"]

            Edge --> API
            Edge --> CA
            Collector --> RawDB
            Worker --> RawDB
            Worker --> AppDB
            Worker --> Data
            Recovery --> RawDB
            Recovery --> AppDB
            Recovery --> Data
            HostCtrl --> Recovery
            HostCtrl --> Worker
            HostCtrl --> Journal
            API --> AppDB
            API --> Journal
            RawDB --> Data
            AppDB --> Data
        end
    end

    Hypixel[Internet: Hypixel API]
    GHCR[External: GitHub Container Registry\nMerged-SHA immutable images]
    Pages[External: GitHub Pages\nSanitized public documentation]

    Browser -->|Local HTTPS through stable name or recorded fallback| Edge
    Browser -.->|Separate hardened SSH management, no data transfer| HostCtrl
    Collector -->|Outbound HTTPS only| Hypixel
    GHCR -->|Digest-pinned pulls| Laptop
    Data -->|Bounded download source| API
    Edge -->|Authenticated signed weekly sets| Desktop
    Edge -->|Authenticated versioned dataset download| Training
    Training -->|Reviewed models through HTTPS edge| Edge
    Pages -.->|Public docs only, no runtime access| Browser
```

The two PostgreSQL containers are explicitly bounded. Normal container ceilings
total about 3 GiB, preserving host/filesystem-cache headroom; swap is not counted
as safe working memory. Host deployment controller stops Worker before starting
the on-demand Recovery profile and mounts recovery credentials only there. No
application container receives Docker socket access.

## Target Trust Boundaries

- Hypixel responses are untrusted external input.
- LAN access does not replace authentication or HTTPS.
- Caddy is the only inbound application/data edge. Hardened SSH is a separate
  administrator management boundary used for deployment and break-glass commands
  only; recovery artifacts never transfer through SSH.
- Recovery worker and storage expose no LAN listener; download/upload/model/data
  transfers cross Caddy and authenticated API.
- API can access Application PostgreSQL, never Raw PostgreSQL or Docker socket.
- Collector can write Raw PostgreSQL, never Application PostgreSQL.
- Curator has least-privilege access to both databases and Parquet output.
- Recovery receives elevated database/storage access only during guarded jobs.
- Restricted host recovery controller has no network listener, owns fsynced
  journal/start-stop/switch operations, and never enters application containers.
- Backups exclude credentials, sessions, API keys, admin hashes, private keys,
  host access details, and personal settings.
- Model artifacts remain untrusted until Release 2 review accepts them.

## Legacy Appendix: Current System Context

Legacy diagrams explain rescue origin only. Legacy system has no product webpage.

```mermaid
flowchart LR
    Operator[Person: Local operator]
    Hypixel[External: Hypixel Bazaar API]
    Legacy[Legacy system: Hypixel Bazaar Tracker\nNo frontend]

    Operator -->|curl/API requests| Legacy
    Operator -->|Adminer database access| Legacy
    Legacy -->|15-second polling| Hypixel
```

## Legacy Appendix: Current Containers

```mermaid
flowchart LR
    Operator[Person: Operator]
    Hypixel[External: Hypixel API]
    MissingMongo[(Missing dependency: MongoDB)]

    subgraph Legacy[Legacy Docker Compose]
        Fetcher[Fetcher\nPython polling loop]
        Processor[SQL processor\nNewest-row-only normalization]
        API[FastAPI\nHistory endpoints, no frontend]
        Adminer[Adminer\nOnly browser interface]
        DB[(Single PostgreSQL\nRaw JSONB and normalized tables)]
        MongoCode[Undeployed stale Mongo code]
    end

    Fetcher --> Hypixel
    Fetcher --> DB
    Processor --> DB
    API --> DB
    Adminer --> DB
    Operator --> API
    Operator --> Adminer
    MongoCode -.-> MissingMongo
```

## Legacy Appendix: Current Components

```mermaid
flowchart LR
    Hypixel[Hypixel API]

    subgraph Fetcher[Fetcher]
        Request[HTTP request]
        Duplicate[Latest-row duplicate check]
        RawModel[Raw ORM model and create_all]
    end

    subgraph Processor[SQL processor]
        Newest[Newest raw-row selector]
        Normalize[Product and offer ORM normalization]
        Models[Duplicated ORM models and create_all]
    end

    subgraph Api[FastAPI]
        Parse[Time/lookback parsing]
        Routes[Latest/history routes]
        Engine[Per-request engine/session]
        Mirror[Second duplicated ORM model set]
    end

    DB[(PostgreSQL)]

    Hypixel --> Request --> Duplicate --> RawModel --> DB
    DB --> Newest --> Normalize --> Models --> DB
    Parse --> Routes --> Engine --> DB
    Mirror --> Engine
```

## Legacy Appendix: Current Deployment

```mermaid
flowchart LR
    Operator[Operator browser or curl]
    Internet[Hypixel API]

    subgraph Host[Single Docker host]
        Fetcher[Fetcher container]
        Processor[SQL processor container]
        API[API on host port 8001]
        Adminer[Adminer on host port 8080]
        DB[PostgreSQL 15 container]
        Volume[(Named database volume)]
        Stale[Stale Mongo files\nBroken image default]
    end

    Fetcher --> Internet
    Fetcher --> DB
    Processor --> DB
    API --> DB
    Adminer --> DB
    DB --> Volume
    Operator --> API
    Operator --> Adminer
    Stale -.-> Fetcher
```

## Architecture Update Rule

Update C4, SRS, ADRs, migrations, and traceability together whenever accepted
work changes containers, databases, authority, trust boundaries, deployment, or
release ownership.
