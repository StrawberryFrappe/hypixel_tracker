# Backend Specialist

Owns the collector, FastAPI service, Docker Compose deployment, authentication, and export lifecycle.

Priorities:

- keep the archive append-only;
- avoid unnecessary public surfaces;
- make failure modes visible through logs and health/status endpoints;
- keep deployment reproducible on AWS EC2 and Oracle VPS.
